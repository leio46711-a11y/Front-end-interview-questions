
---

## 一、JavaScript 核心（★★★ 必考）
### 1. 原型链与继承机制
**核心答案**：
- 每个对象都有一个 `__proto__` 属性指向它的原型对象
- 每个函数都有一个 `prototype` 属性，是实例的原型
- 当访问对象属性时，会沿着 `__proto__` 向上查找，直到 `Object.prototype`，这条链就是原型链
- ES6 `class` 本质是语法糖，底层仍是原型链继承

**答题要点**：
- 先讲原型和原型链的定义
- 举例说明继承的实现（ES5 构造函数继承 + ES6 class 继承）
- 提到 `Object.create()`、`instanceof` 原理

### 2. 闭包
**核心答案**：
- 闭包是指**有权访问另一个函数作用域中变量的函数**
- 形成条件：函数嵌套 + 内部函数引用外部函数变量
- 作用：实现变量私有化、柯里化、模块化
- 缺点：使用不当会导致内存泄漏（变量无法被垃圾回收）

**答题要点**：
- 举一个简单的闭包例子
- 说明闭包的应用场景（防抖节流、React Hooks）
- 提到内存泄漏问题及解决方案（及时释放引用）

### 3. JavaScript 事件循环机制
**核心答案**：
- JS 是单线程语言，所有任务分为**同步任务**和**异步任务**
- 同步任务在主线程执行栈中执行
- 异步任务放入任务队列，分为**微任务**和**宏任务**
- 执行顺序：同步任务 → 所有微任务 → 一个宏任务 → 循环往复
- 微任务：`Promise.then/catch/finally`、`async/await`、`MutationObserver`
- 宏任务：`setTimeout/setInterval`、`script`、`I/O`、`UI 渲染`

**答题要点**：
- 先讲整体流程，再区分微任务和宏任务
- 举一个代码执行顺序的例子
- 提到 Node.js 事件循环与浏览器的区别（阶段不同）

### 4. `setTimeout` 为什么不准确？如何实现精准倒计时？
**核心答案**：
- 不准确原因：
  1. JS 单线程，若主线程阻塞，回调会延迟执行
  2. 浏览器最小时间精度限制（通常 4ms）
  3. 页面进入后台时，定时器会被降频
- 精准倒计时实现：
  1. 用 `Date.now()` 计算时间差，动态修正下一次定时器的延迟
  2. 结合 `requestAnimationFrame` 提升精度
  3. 异常弱网下，使用服务器时间校准

---

## 二、浏览器原理（★★★ 必考）
### 1. 输入 URL 到页面展示的完整过程
**核心答案**：
1. **DNS 解析**：将域名解析为 IP 地址（浏览器缓存 → 系统缓存 → 本地 DNS → 根 DNS → 顶级 DNS）
2. **TCP 连接**：三次握手建立连接（HTTP/2 复用连接）
3. **HTTP 请求**：发送请求行、请求头、请求体
4. **服务器响应**：返回状态码、响应头、响应体
5. **浏览器渲染**：
   - 解析 HTML → DOM 树
   - 解析 CSS → CSSOM 树
   - 合并 DOM + CSSOM → 渲染树
   - 布局（回流）：计算节点位置和大小
   - 绘制（重绘）：将像素绘制到屏幕
   - 合成：将图层合并为最终页面
6. **断开连接**：四次挥手关闭 TCP 连接

**答题要点**：
- 按顺序讲，重点突出渲染流程
- 提到 HTTP/2 的多路复用优化
- 结合回流重绘的知识点

### 2. 回流（重排）与重绘
**核心答案**：
- **回流**：当元素的布局属性（宽高、位置、大小）发生变化时，浏览器重新计算布局的过程
- **重绘**：当元素的样式属性（颜色、背景）发生变化时，浏览器重新绘制像素的过程
- 关系：回流一定会触发重绘，重绘不一定触发回流
- 优化手段：
  1. 批量修改 DOM（使用文档碎片、离线 DOM）
  2. 避免频繁读取布局属性（浏览器会强制刷新队列）
  3. 使用 `transform` 和 `opacity` 实现动画（只触发合成层，不回流重绘）
  4. 开启 GPU 加速

### 3. 浏览器缓存策略
**核心答案**：
- 分为**强缓存**和**协商缓存**，优先级：强缓存 > 协商缓存
- **强缓存**：
  - 响应头：`Cache-Control: max-age=xxx`（秒）、`Expires`（绝对时间，已废弃）
  - 命中条件：缓存未过期，直接从本地读取，不发请求
- **协商缓存**：
  - 响应头：`Last-Modified` / `If-Modified-Since`、`ETag` / `If-None-Match`
  - 命中条件：缓存已过期，发请求到服务器验证，资源未变则返回 304，使用本地缓存
  - 未命中：返回 200 和新资源

### 4. 页面卸载时如何发送埋点数据？
**核心答案**：
- 不能用普通 AJAX：页面卸载时会取消未完成的请求
- 推荐方案：`navigator.sendBeacon(url, data)`
  - 异步发送，不阻塞页面卸载
  - 数据会被浏览器可靠地发送
  - 限制：只能发送 POST 请求，数据量有限（约 64KB）
- 备选方案：创建隐藏的 `<img>` 标签，设置 src 为埋点地址

---

## 三、网络协议（★★★ 必考）
### 1. 跨域的原理及解决方案
**核心答案**：
- 跨域原因：浏览器的**同源策略**（协议、域名、端口三者必须一致）
- 常见解决方案：
  1. **CORS**：服务端设置 `Access-Control-Allow-Origin` 等响应头，最推荐
  2. **代理服务器**：前端请求本地代理，代理转发到目标服务器（如 Vite proxy、Nginx 反向代理）
  3. **JSONP**：利用 `<script>` 标签不受同源策略限制，只支持 GET 请求
  4. **WebSocket**：不受同源策略限制
  5. **postMessage**：跨窗口通信

**答题要点**：
- 先讲同源策略的定义
- 重点讲 CORS 和代理服务器，说明优缺点
- 提到 OPTIONS 预检请求

### 2. 什么是 OPTIONS 预检请求？什么时候触发？
**核心答案**：
- OPTIONS 是一种 HTTP 方法，用于向服务器查询资源支持的请求方法和头信息
- 触发条件（非简单请求）：
  1. 请求方法不是 GET/POST/HEAD
  2. 请求头包含自定义字段（如 `Content-Type: application/json`）
  3. 使用了自定义请求头
- 作用：浏览器在发送真实请求前，先发送 OPTIONS 请求，确认服务器是否允许跨域，避免真实请求被拒绝

### 3. HTTP/2.0 核心特性
**核心答案**：
1. **二进制分帧**：将数据拆分为二进制帧，并行传输，解决 HTTP/1.1 的队头阻塞
2. **多路复用**：同一个 TCP 连接上可以同时发送多个请求和响应，无需排队
3. **头部压缩**：使用 HPACK 算法压缩请求头和响应头，减少数据量
4. **服务器推送**：服务器可以主动向客户端推送资源，无需客户端请求
5. **流优先级**：可以设置请求的优先级，重要资源优先传输

### 4. SSE 与 WebSocket 的区别
**核心答案**：

| 特性 | SSE（Server-Sent Events） | WebSocket |
|------|----------------------------|-----------|
| 通信方式 | 单向通信（服务器 → 客户端） | 双向全双工通信 |
| 协议 | 基于 HTTP 协议 | 独立的 WebSocket 协议（ws/wss） |
| 连接 | 长连接，自动重连 | 长连接，需手动实现重连 |
| 数据格式 | 纯文本 | 二进制、文本 |
| 适用场景 | 实时日志、消息推送、流式输出 | 聊天、游戏、实时协作 |

**答题要点**：
- 重点讲通信方式和适用场景
- 提到 SSE 原生支持自动重连，但异常情况下仍需手动重试（如网络断连超过 30s）

---

## 四、Vue 框架（★★★ 必考）
### 1. Vue2 与 Vue3 响应式原理区别
**核心答案**：
- **Vue2**：基于 `Object.defineProperty`
  - 初始化时递归遍历所有属性，为每个属性设置 getter/setter
  - 缺陷：无法监听对象新增/删除属性、数组下标/长度变更
  - 解决：`Vue.set`/`Vue.delete`、重写 7 个数组变异方法
- **Vue3**：基于 `Proxy` + `Reflect`
  - 懒代理：只代理顶层对象，访问嵌套属性时才递归代理
  - 优势：支持对象新增/删除、数组下标变更、Map/Set 监听
  - 核心 API：`ref`（基本类型）、`reactive`（引用类型）

### 2. Vue Diff 算法原理
**核心答案**：
- **通用规则**：同层比较，不跨层级移动 DOM；标签不同直接销毁重建
- **Vue2**：双端比较算法
  - 维护新旧列表首尾四个指针，双端交叉比对
  - 未命中则通过 key 查找节点，移动 DOM
- **Vue3**：最长递增子序列算法
  - 编译时标记动态节点（PatchFlag），Diff 只对比动态节点
  - 运行时计算最长递增子序列，序列内节点不移动，只移动序列外节点
  - 大幅减少 DOM 移动次数，长列表性能提升明显

### 3. Composition API 与 Options API 的区别
**核心答案**：
- **Options API**：按选项类型分隔代码（data、methods、computed）
  - 优点：简单易上手，符合直觉
  - 缺点：复杂组件逻辑碎片化，复用困难（mixins 有命名冲突）
- **Composition API**：按业务逻辑聚合代码
  - 优点：逻辑清晰，便于复用（组合式函数），天然支持 TypeScript
  - 缺点：需要一定学习成本

### 4. Vue `scoped` 样式实现原理
**核心答案**：
- 编译时给组件内所有 DOM 元素添加唯一的 `data-v-xxx` 属性
- 编译时给 CSS 选择器添加对应的属性选择器（如 `.class[data-v-xxx]`）
- 实现样式隔离，只作用于当前组件内的元素
- 注意：子组件的根元素会继承父组件的 scoped 样式

---

## 五、React 框架（★★★ 必考）
### 1. Fiber 架构与并发渲染原理
**核心答案**：
- **Fiber 解决的问题**：React 15 之前的栈调和是同步执行的，长任务会阻塞主线程，导致页面卡顿
- **Fiber 核心思想**：将渲染任务拆分为多个小任务，可中断、可恢复、可优先级调度
- **Fiber 节点**：每个 DOM 节点对应一个 Fiber 节点，包含节点信息、指针、优先级等
- **执行流程**：
  1. 协调阶段（可中断）：遍历 Fiber 树，生成 Effect List（副作用列表）
  2. 提交阶段（不可中断）：执行 Effect List，更新 DOM
- **并发渲染**：多个更新任务按优先级调度，高优先级任务可以打断低优先级任务

### 2. React Hooks 为什么不能在条件判断和循环中使用？
**核心答案**：
- Hooks 底层是**链表**存储的，按调用顺序依次挂载
- 如果在条件判断或循环中使用 Hooks，会导致链表顺序错乱
- 当组件重新渲染时，Hooks 的调用顺序必须与上一次完全一致，否则会获取到错误的状态
- 解决方案：将条件判断移到 Hook 内部

### 3. React Diff 算法核心策略
**核心答案**：
- 同层比较，不跨层级移动 DOM
- 标签不同：直接销毁旧节点，新建新节点
- 标签相同：对比属性，更新属性
- 列表节点：通过 key 唯一标识节点，复用现有 DOM
- 与 Vue Diff 区别：
  - 相同：同层比较、key 复用
  - 不同：Vue3 用最长递增子序列优化 DOM 移动；React 用双端遍历 + 索引映射

### 4. React 性能优化方式
**核心答案**：
- **渲染优化**：
  1. `React.memo`：缓存组件，避免不必要的重渲染
  2. `useMemo`：缓存计算结果
  3. `useCallback`：缓存函数引用
  4. 避免在 render 中创建新对象/数组
- **代码分割**：`React.lazy` + `Suspense` 实现按需加载
- **状态管理优化**：
  1. 状态局部化，避免全局状态滥用
  2. 使用不可变数据结构
- **其他**：使用虚拟列表优化长列表、使用 `transform` 实现动画

---

## 六、工程化（★★★ 必考）
### 1. Webpack 完整构建流程
**核心答案**：
1. **初始化**：读取配置文件，合并命令行参数，创建 Compiler 实例
2. **编译**：
   - 从入口文件开始，递归解析所有依赖
   - 调用对应 Loader 转换文件（如 babel-loader 转译 JS）
   - 生成 AST 抽象语法树，分析依赖关系
3. **构建**：将所有模块打包成 Chunk
4. **优化**：压缩代码、Tree-Shaking、代码分割
5. **输出**：将 Chunk 输出到指定目录

### 2. Plugin 与 Loader 的核心区别
**核心答案**：
- **Loader**：
  - 作用：转换模块内容（将非 JS 文件转换为 JS 模块）
  - 执行时机：编译阶段，在模块加载时执行
  - 例子：babel-loader、css-loader、file-loader
- **Plugin**：
  - 作用：扩展 Webpack 功能，执行更广泛的任务
  - 执行时机：贯穿整个构建生命周期（初始化、编译、构建、输出）
  - 例子：HtmlWebpackPlugin、CleanWebpackPlugin、TerserPlugin

### 3. Vite 为什么比 Webpack 快？
**核心答案**：
1. **开发环境**：
   - 基于 ESBuild 预构建依赖，速度是 Webpack 的 10-100 倍
   - 采用**原生 ESM** 加载模块，浏览器接管打包工作，按需加载
   - HMR 热更新只更新修改的模块，速度与项目体积无关
2. **生产环境**：
   - 使用 Rollup 打包，天生支持 Tree-Shaking
   - 内置代码分割、压缩等优化
3. **架构差异**：Webpack 是全量打包，Vite 是按需编译

### 4. 如何优化 Webpack 构建速度和打包体积？
**核心答案**：
- **构建速度优化**：
  1. 使用更快的 Loader（如 babel-loader 开启缓存）
  2. 多线程构建（thread-loader）
  3. 缩小搜索范围（resolve.alias、exclude）
  4. 开启缓存（cache: filesystem）
- **打包体积优化**：
  1. Tree-Shaking：剔除未使用的代码
  2. 代码分割：按路由、第三方库分割
  3. 压缩代码：TerserPlugin 压缩 JS，css-minimizer-webpack-plugin 压缩 CSS
  4. 图片压缩、字体优化
  5. 使用 CDN 加载第三方库

---

## 七、性能优化与监控（★★★ 高频）
### 1. 前端核心性能指标
**核心答案**：
- **FP（First Paint）**：首次绘制，浏览器第一次渲染像素的时间
- **FCP（First Contentful Paint）**：首次内容绘制，页面首次渲染文本/图片的时间
- **LCP（Largest Contentful Paint）**：最大内容绘制，页面最大元素渲染完成的时间（<2.5s 优秀）
- **CLS（Cumulative Layout Shift）**：累积布局偏移，页面元素意外移动的总和（<0.1 优秀）
- **TBT（Total Blocking Time）**：总阻塞时间，主线程被阻塞的总时长（<300ms 优秀）
- **FID（First Input Delay）**：首次输入延迟，用户首次交互到浏览器响应的时间（<100ms 优秀）

### 2. 虚拟列表的实现原理（动态高度）
**核心答案**：
- 核心思想：只渲染可视区域内的元素，滚动时动态替换内容
- 实现步骤：
  1. 计算可视区域高度和每项的预估高度，得到可视区项数
  2. 监听滚动事件，计算当前滚动位置对应的起始索引和结束索引
  3. 只渲染起始索引到结束索引之间的元素
  4. 动态高度处理：
     - 渲染后获取真实高度，更新高度缓存
     - 重新计算滚动偏移量，避免跳动
     - 使用最长递增子序列优化 DOM 复用

### 3. 前端监控体系设计
**核心答案**：
- **错误监控**：
  - JS 错误：`window.onerror`、`window.addEventListener('error')`
  - 资源加载错误：`window.addEventListener('error', true)`
  - Promise 错误：`window.addEventListener('unhandledrejection')`
- **性能监控**：
  - 使用 `Performance API` 采集 FP、FCP、LCP 等指标
  - 采集页面加载时间、接口请求时间
- **用户行为监控**：
  - 采集 PV、UV、点击事件、页面停留时间
- **异常弱网监控**：
  - 采集接口成功率、超时率、丢包率
- **上报策略**：
  - 批量上报、节流上报
  - 使用 `navigator.sendBeacon` 保证上报可靠性

---

## 八、场景题与架构设计（★★★ 高频）
### 1. SSE 流式输出如何应对异常弱网？
**核心答案**：
1. **自动重连优化**：
   - 原生 SSE 自动重连间隔是 3s，可通过 `retry` 字段自定义
   - 实现指数退避重连（1s → 2s → 4s → 最大 30s）
   - 重连时携带上次接收的最后一个 ID，服务器从断点继续发送
2. **数据缓存与去重**：
   - 前端缓存已接收的 chunk，重连后去重
   - 每个 chunk 带唯一 ID，避免重复渲染
3. **超时处理**：
   - 设置心跳检测，长时间无数据则主动重连
4. **降级策略**：
   - 弱网下自动降级为轮询
   - 显示加载状态和网络提示

### 2. 前端任务调度系统设计
**核心答案**：
- 核心需求：按优先级调度任务，避免阻塞主线程
- 实现方案：
  1. 使用 `requestIdleCallback` 在浏览器空闲时执行低优先级任务
  2. 使用 `requestAnimationFrame` 执行高优先级任务（如动画）
  3. 实现任务队列，按优先级排序
  4. 任务分片：将长任务拆分为多个小任务，分批执行
  5. 可中断：支持任务暂停和恢复

---

## 九、答题技巧
1. **项目讲解 STAR 法则**：
   - S（Situation）：项目背景
   - T（Task）：你的任务和职责
   - A（Action）：你做了什么，用了什么技术
   - R（Result）：取得了什么成果（最好有数据支撑）
2. **答题逻辑**：先讲核心结论，再讲细节，最后讲优缺点和优化方向
3. **突出优势**：结合自己的项目经验，展示解决问题的能力
4. **不懂就说**：遇到不会的问题，诚实说明，然后说自己的思路和学习方法
